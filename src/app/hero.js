import React from 'react'

const hero = ({movies}) => {
    const bkg = movies[0].backdrop_path;
  return (
    <section>
        <div>

        </div>
    </section>
    
  )
}

export default hero

